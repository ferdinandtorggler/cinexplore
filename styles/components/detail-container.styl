.detail-container
    // 205 = movie header
    // 46 = tab bar
    // 48 = top bar
    height calc(100vh - 260px - 48px)
    overflow auto

    h1
        font-family font-main
        font-size font-size
        margin 0